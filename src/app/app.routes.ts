import { Routes } from '@angular/router';
import { UserFormComponent } from './pages/user/user.component';

export const routes: Routes = [
    {
        path: '',
        component: UserFormComponent
    }
];
